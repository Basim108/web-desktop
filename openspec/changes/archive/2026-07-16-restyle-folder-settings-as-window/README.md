# restyle-folder-settings-as-window

Restyle folder settings from an anchored popup into a modal window matching the Edit Bookmark window (staged Save, rename, remove folder); bump the gear button to 16px
