name: Security Scan

on:
  push:
    paths:
      - "src/**"
      - ".github/**"
  schedule:
    # Weekly, matching Dependabot's cadence (.github/dependabot.yml).
    - cron: "0 3 * * 1"
  workflow_dispatch: {}

jobs:
  security-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Retire.js — known-vulnerable JS library scan
        run: npm run security:retire -- --outputformat text

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Semgrep
        run: pip install semgrep

      - name: Semgrep — dangerous-function/pattern scan
        run: |
          semgrep --error \
            --config p/security-audit \
            --config p/javascript \
            --config p/typescript \
            --exclude node_modules --exclude dist \
            --exclude "*.test.*" --exclude e2e \
            src/ manifest.config.ts
